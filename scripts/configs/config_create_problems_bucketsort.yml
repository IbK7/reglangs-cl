save_folder:
  "uniform-length_smol_BucketSort_42"

problem:
    "bucketsort"

sampling:
  "uniform-length"

problem_file:
  "bucketsort.yml"

parallel:
  False

n_jobs:
  20

dataset:
  # expects total number of samples, usually 20,000,000
  training_set_size:
    10_000
  # expects samples per length, usually 100
  unseen_batch_size:
    10
  max_train_length:
    40
  max_length:
    500
  num_problems:
    1
  seed:
    42

